/**
 * Stepper — .stepper number control used for the timer durations.
 * Props: { value, onChange, min = 1, max = 90, step = 1, suffix }
 * BUILD: clamp on both buttons and disable them at the bounds.
 */

export default function Stepper() {
  return null; // TODO
}
