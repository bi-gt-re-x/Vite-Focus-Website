/**
 * ProgressBar — .progress + .progress__bar.
 * Props: { value, max = 100, small, color }
 * BUILD: width = `${(value / max) * 100}%`; add role="progressbar"
 * with aria-valuenow / aria-valuemax.
 */

export default function ProgressBar() {
  return null; // TODO
}
