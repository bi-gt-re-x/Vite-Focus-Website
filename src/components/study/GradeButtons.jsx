/**
 * GradeButtons — Again / Hard / Good / Easy.
 * Props: { onGrade, intervals }
 *
 * `intervals` is what nextIntervals() in utils/scheduler.js returns for the
 * current card — show it in the <small> so the user sees when it comes back.
 * Grades are 0-3; keys 1-4 trigger them (wire that in StudyPage, not here).
 */

export default function GradeButtons() {
  return null; // TODO
}
