/**
 * Sidebar — brand, nav links, streak footer.
 * Markup: mockups/_shell.html
 *
 * BUILD:
 *  - Define a NAV array [{ to, label, icon, badge }] and map it.
 *  - Use react-router's <NavLink> and its isActive render prop to add
 *    "nav-link--active".
 *  - The Decks badge shows DECKS.length; the streak comes from useProgress().
 *  - Keep the SVGs as small components or inline — they inherit currentColor.
 */

export default function Sidebar() {
  return null; // TODO
}
