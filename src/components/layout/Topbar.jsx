/**
 * Topbar — page title, mini timer, theme toggle.
 *
 * BUILD:
 *  - Props: { title }.
 *  - The mini timer reads the running timer from context and only renders
 *    when status !== 'idle'. Set data-running={status === 'running'} so the
 *    dot animates.
 *  - The theme button cycles through THEMES (data/themes.js) or opens
 *    the settings page — your call.
 */

export default function Topbar({ title }) {
  return null; // TODO
}
