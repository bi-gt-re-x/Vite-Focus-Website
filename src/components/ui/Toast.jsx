/**
 * Toast + ToastRegion — .toast-region / .toast.
 *
 * BUILD:
 *  - A small provider holding an array of toasts and exposing push(toast).
 *  - Each toast auto-dismisses after ~4s (clear the timeout on unmount).
 *  - Render the region into the shell so it floats above every page.
 *  - Give the region aria-live="polite" so screen readers announce it.
 */

export function ToastRegion() {
  return null; // TODO
}
