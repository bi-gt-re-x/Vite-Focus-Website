/**
 * Toggle — the .switch markup (label > input + span.switch__track).
 * Props: { checked, onChange, label }
 * Keep the real <input type="checkbox"> — it gives you keyboard support free.
 */

export default function Toggle() {
  return null; // TODO
}
