/**
 * SessionSummary — the .session-complete panel.
 * Props: { results, deck, onRetryMissed, onExit }
 *
 * `results` is the array of { cardId, grade } collected during the session.
 * Derive the score, the three-way breakdown, and the list of missed cards here.
 */

export default function SessionSummary() {
  return null; // TODO
}
