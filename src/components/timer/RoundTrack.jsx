/**
 * RoundTrack — the row of dots showing progress toward the long break.
 * Props: { completed, total, current }
 * BUILD: render `total` dots; add --done below current and --current on it.
 */

export default function RoundTrack() {
  return null; // TODO
}
