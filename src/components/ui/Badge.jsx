/**
 * Badge — .badge with a tone modifier.
 * Props: { tone = 'default'|'accent'|'success'|'warning'|'danger'
 *                 |'easy'|'medium'|'hard', dot, children }
 * The difficulty tones map straight onto card.difficulty.
 */

export default function Badge({ children }) {
  return null; // TODO
}
