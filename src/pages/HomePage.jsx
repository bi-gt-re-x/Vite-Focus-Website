/**
 * HomePage — dashboard.
 * Markup: mockups/home.html
 *
 * BUILD:
 *  - Hero: greeting from the current hour, a "start focus" link to /timer,
 *    and a "review N due cards" link to the deck with the most due cards.
 *  - Four stat tiles from useStats().
 *  - "Jump back in": DECKS sorted by due count, top 4.
 *  - "Recent activity": the last five entries of progress.sessions, newest first,
 *    with times via formatTime() from utils/time.js.
 */

export default function HomePage() {
  return null; // TODO
}
