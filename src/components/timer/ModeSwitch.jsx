/**
 * ModeSwitch — Focus / Short break / Long break tabs.
 * Props: { mode, onChange }
 * BUILD: role="tablist" on the wrapper, aria-selected on each button.
 * Switching mode while a timer runs should ask for confirmation first.
 */

export default function ModeSwitch() {
  return null; // TODO
}
