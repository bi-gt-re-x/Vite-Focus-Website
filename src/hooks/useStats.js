/**
 * useStats — derives every number the Home and Stats pages show.
 *
 * Returns { focusToday, pomodorosToday, cardsToday, accuracy, streak,
 *           heatmap, weekMinutes, accuracyByDeck, hardestCards }
 *
 * BUILD: pure derivation from ProgressContext, wrapped in useMemo keyed on
 * the progress object. No state of its own, no effects.
 */

export function useStats() {
  // TODO
}
