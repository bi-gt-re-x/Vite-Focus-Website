/**
 * Button — thin wrapper over the .btn classes.
 *
 * Props: { variant = 'primary' | 'secondary' | 'ghost' | 'danger',
 *          size = 'sm' | 'md' | 'lg', icon, block, ...rest }
 *
 * BUILD: compose the className string, spread ...rest onto <button>
 * so onClick, disabled, type and aria-* all pass through.
 */

export default function Button({ children }) {
  return null; // TODO
}
