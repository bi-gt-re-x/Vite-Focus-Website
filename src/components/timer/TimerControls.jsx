/**
 * TimerControls — reset · play/pause · skip.
 * Props: { status, onToggle, onReset, onSkip }
 * BUILD: swap the pause rects for the play triangle when status !== 'running',
 * and keep aria-label in sync with what the button will do.
 */

export default function TimerControls() {
  return null; // TODO
}
