/**
 * useTheme — read/set the active theme.
 * Returns { theme, setTheme, cycleTheme, themes }
 *
 * Applying a theme is one line:
 *   document.documentElement.dataset.theme = theme;
 * Everything else is CSS. Persist through SettingsContext, not a second store.
 */

export function useTheme() {
  // TODO
}
