/**
 * Time formatting helpers. Pure functions — easy to test.
 *
 * formatClock(totalSeconds)  ->  "18:42"   (mm:ss, zero padded)
 * formatDuration(minutes)    ->  "2h 05m"
 * formatTime(date)           ->  "14:20"
 * minutesToSeconds(min)      ->  min * 60
 * isSameDay(a, b)            ->  boolean
 * daysBetween(a, b)          ->  integer
 */

export function formatClock() {
  // TODO
}
