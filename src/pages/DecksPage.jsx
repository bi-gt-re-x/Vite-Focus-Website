/**
 * DecksPage — the deck library.
 * Markup: mockups/decks.html
 *
 * BUILD:
 *  - Map DECKS; put style={{ '--deck-color': deck.color }} on each .deck-card.
 *  - Search filters on deck name AND on card question text (cardsByDeck).
 *  - Tabs filter All / Due / In progress; chips filter by tag (allTags).
 *  - Mastery % = cards graded Good or Easy at least once / total cards.
 *  - Render <EmptyState> when the filtered list is empty.
 *  - Derive the filtered list during render — no useEffect, no mirror state.
 */

export default function DecksPage() {
  return null; // TODO
}
